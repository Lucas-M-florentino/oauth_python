

Gerar secret:

no terminal digitar python
$ python
>>> import os
>>> import binascii
>>> binascii.hexlify(os.random(24))


Editor python no terminal com autocomplete:

instalar com pip:
$ pip install ipython

executar no terminal:
$ ipython